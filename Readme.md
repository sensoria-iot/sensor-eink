### EPDiy as a component

This is the most basic setup possible to add epdiy and possible another components working together in esp-idf

Just clone with submodules:

     git clone REPO_URL --recursive